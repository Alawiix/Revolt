# UTF-8
#
# Windows file-version resource for Revolt.
# Feed this to PyInstaller with:
#   pyinstaller --onefile --manifest revolt.exe.manifest --version-file version.txt --icon=your_icon.ico revolt_app_tkniter.py
#
# This is what shows up in the exe's Properties > Details tab in Explorer,
# and in the UAC prompt when the app elevates.

VSVersionInfo(
  ffi=FixedFileInfo(
    # filevers and prodvers should be always a tuple with four items: (1, 2, 3, 4)
    # Set not needed items to zero 0.
    filevers=(1, 0, 7, 0),
    prodvers=(1, 0, 7, 0),
    # Contains a bitmask that specifies the valid bits 'flags'
    mask=0x3f,
    # Contains a bitmask that specifies the Boolean attributes of the file.
    flags=0x0,
    # The operating system for which this file was designed.
    # 0x4 - NT and there is no need to change it.
    OS=0x40004,
    # The general type of file.
    # 0x1 - the file is an application.
    fileType=0x1,
    # The function of the file.
    # 0x0 - the function is not defined for this fileType
    subtype=0x0,
    # Creation date and time stamp.
    date=(0, 0)
    ),
  kids=[
    StringFileInfo(
      [
      StringTable(
        u'040904B0',
        [StringStruct(u'CompanyName', u'NCZ'),
        StringStruct(u'FileDescription', u'Revolt - content blocking & parental controls'),
        StringStruct(u'FileVersion', u'1.0.7.0'),
        StringStruct(u'InternalName', u'Revolt'),
        StringStruct(u'LegalCopyright', u'\u00A9 2026 NCZ. All rights reserved.'),
        StringStruct(u'OriginalFilename', u'Revolt.exe'),
        StringStruct(u'ProductName', u'Revolt'),
        StringStruct(u'ProductVersion', u'1.0.7.0')])
      ]),
    VarFileInfo([VarStruct(u'Translation', [1033, 1200])])
  ]
)
